using UnityEngine;

/*public class Button3D : MonoBehaviour
{
    public enum ButtonType { Hit, Stand }
    public ButtonType buttonType;

    public CardManager cardManager; // Assign via Inspector

    void OnMouseDown()
    {
        if (buttonType == ButtonType.Hit)
        {
            cardManager.PlayerHit();
        }
        else if (buttonType == ButtonType.Stand)
        {
            cardManager.PlayerStand();
        }
    }
}
*/